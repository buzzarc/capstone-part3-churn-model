# Part 3: Churn Prediction Model & Model Card

This repository contains the model development pipeline, diagnostic logs, performance matrix records, and validation sets.

## Execution
1. Install dependencies: `pip install -r requirements.txt`
2. Train model and generate metrics: `python -c "import churn_model; churn_model.train_pipeline()"`